//UNITY_SHADER_NO_UPGRADE



float valuesToIndex_float(float resolution,float valueone, float valuetwo, out float index)
{
    
    
    return index = (resolution * floor(valuetwo+1)) + floor(valueone);
}